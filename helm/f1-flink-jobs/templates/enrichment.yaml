{{- if .Values.jobs.enrichment.enabled }}
apiVersion: flink.apache.org/v1beta1
kind: FlinkDeployment
metadata:
  name: f1-lap-enrichment
  namespace: {{ .Release.Namespace }}
spec:
  image: {{ .Values.image.repository }}:{{ .Values.image.tag }}
  flinkVersion: v{{ .Values.flink.version | replace "." "_" }}
  flinkConfiguration:
    taskmanager.numberOfTaskSlots: "1"
    state.checkpoints.dir: file:///flink-data/checkpoints
  serviceAccount: flink
  jobManager:
    resource:
      memory: "1024m"
      cpu: 1
  taskManager:
    replicas: {{ .Values.jobs.enrichment.taskManager.replicas }}
    resource:
      memory: {{ .Values.jobs.enrichment.taskManager.resource.memory | quote }}
      cpu: {{ .Values.jobs.enrichment.taskManager.resource.cpu }}
  job:
    jarURI: local:///opt/flink/usrlib/flink-jobs.jar
    entryClass: {{ .Values.jobs.enrichment.mainClass }}
    parallelism: {{ .Values.jobs.enrichment.parallelism }}
    upgradeMode: stateless
  podTemplate:
    spec:
      containers:
        - name: flink-main-container
          env:
            - name: KAFKA_BOOTSTRAP_SERVERS
              value: {{ .Values.kafka.bootstrapServers | quote }}
{{- end }}
